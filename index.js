// Define the currentUser variable
const currentUser = "Grace Hopper";

// Create the welcomeMessage using the currentUser
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Create the excitedWelcomeMessage in uppercase and with an exclamation mark
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Create the shortGreeting using only the first initial of currentUser
const shortGreeting = `Welcome, ${currentUser[0]}!`;

